return {
  ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc", "bash", "python", "toml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}
